server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location /buildbanner.json {
        default_type application/json;
        add_header Cache-Control "no-store";
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
