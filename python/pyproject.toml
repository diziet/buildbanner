[project]
name = "buildbanner"
version = "0.1.0"
description = "BuildBanner server helpers for Flask, Django, and FastAPI"
requires-python = ">=3.8"

[project.optional-dependencies]
test = ["pytest"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
